[[hooks]]
stage = "pre_build"
command = "tailwindcss"
command_arguments = [
  "-i", "style/main.css", 
  "-o", "style/output.css",
]

[build]
target = "index.html"
release = false
dist = "dist"
public_url = "/"

[watch]
watch = ["src", "style", "index.html"]

[serve]
port = 8080
open = true 
