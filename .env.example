# Example Environment Variables for EASM

# -- Application Configuration --
# Environment: development, production, or test
ENVIRONMENT=development
# Host IP address to bind the API server to
HOST=127.0.0.1
# Port to bind the API server to
PORT=3000
# Log level: trace, debug, info, warn, error
LOG_LEVEL=info

# -- Database Configuration --
# PostgreSQL connection URL
DATABASE_URL="postgres://postgres:postgres@localhost:5432/easm"

# -- Redis Configuration (Optional) --
# If using Redis for caching or session management
# REDIS_URL="redis://127.0.0.1/"

# -- Authentication (JWT) --
# VERY IMPORTANT: Replace with a strong, randomly generated secret!
JWT_SECRET="changeme_very_secret_key_please_replace"
# JWT expiration time in seconds (default: 86400 = 24 hours)
JWT_EXPIRATION=86400

# -- Task Worker Configuration --
# Maximum number of discovery tasks to run concurrently
MAX_CONCURRENT_TASKS=10

# -- Discovery Module Configuration (Optional Examples) --
# Default timeout for port scans in seconds
# PORT_SCAN_TIMEOUT_SECS=30
# User-Agent for web crawler
# WEB_CRAWLER_USER_AGENT="EASM Bot/1.0 (+https://example.com/bot)"
# API Key for Shodan (REPLACE and DO NOT COMMIT if used)
# SHODAN_API_KEY="YOUR_SHODAN_API_KEY"

''' 